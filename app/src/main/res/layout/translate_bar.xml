<?xml version="1.0" encoding="utf-8"?>
<!--
    SPDX-License-Identifier: GPL-3.0-only
    Integrated translator bar shown above the keyboard when translate mode is active.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/translate_bar_root"
    android:layout_width="match_parent"
    android:layout_height="@dimen/config_suggestions_strip_height"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    style="?attr/suggestionStripViewStyle"
    tools:ignore="MissingDefaultResource">

    <Spinner
        android:id="@+id/spinner_source"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:minHeight="0dp" />

    <ImageButton
        android:id="@+id/btn_swap"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?android:attr/textColorPrimary"
        android:src="@drawable/ic_swap"
        android:contentDescription="@string/translate_swap" />

    <Spinner
        android:id="@+id/spinner_target"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:minHeight="0dp" />

    <TextView
        android:id="@+id/text_result"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="5"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="@string/translate_result_placeholder"
        android:textColor="?android:attr/textColorPrimary"
        android:clickable="true"
        android:focusable="true" />

    <ImageButton
        android:id="@+id/btn_insert"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?android:attr/textColorPrimary"
        android:src="@drawable/ic_check"
        android:contentDescription="@string/translate_insert" />

    <ImageButton
        android:id="@+id/btn_settings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?android:attr/textColorPrimary"
        android:src="@drawable/ic_settings_gear"
        android:contentDescription="@string/translate_settings" />

    <ImageButton
        android:id="@+id/btn_close"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?android:attr/textColorPrimary"
        android:src="@drawable/ic_close"
        android:contentDescription="@string/translate_close" />
</LinearLayout>
